# Startup script for the Monasca API

description "Monasca API java app"
start on runlevel [2345]

console log
respawn

setgid monasca
setuid monasca_api
exec /usr/bin/java -Xmx8g -cp {{ monasca_jar_dir }}/monasca-api.jar:{{ monasca_jar_dir }}/vertica/vertica_jdbc.jar monasca.api.MonApiApplication server {{api_conf_file}}
